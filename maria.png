.item_menu a{
    color: var(--azul_escuro);
}

/*
.item_menu a:hover{
    color: var(--amarelo_padrao);
} */

.pagina_azul {
    /* background-color: var(--azul_padrao); */
    background-color: #EFEFEF;
}

#main_projeto {
    display: flex;
    align-items: center;
    justify-content: center;
}

#section_projeto, #section_box {
    width: 90%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: justify;
}

#section_projeto h1 {
    margin: 7rem 0;
    font-size: 3.5rem;
    color: var(--azul_padrao);
    border-bottom: solid .5rem var(--azul_padrao);
}

#section_projeto p, #box_descricao p {
    font-family: 'Montserrat', sans-serif;
    padding: 1rem 0;
    font-size: 1.9rem;
    font-weight: 400;
    color: var(--azul_escuro);
}

#section_box {
    margin: 5rem 0;
}

#section_box h2 {
    margin: .5rem 0;
    letter-spacing: .2rem;
    font-weight: 400;
    padding: .5rem 2rem;
    font-size: 3rem;
    color: var(--azul_padrao);
    border-bottom: solid .5rem var(--azul_padrao);
}

#box_descricao {
    margin: 3rem 0 1rem 0;
    text-align: center;
}

#box_itens {
    display: flex;
    justify-content: space-around;
    align-items: center;
    width: 100%;
    flex-wrap: wrap;
    flex-direction: row;
}

.box_itens_item {
    width: 25rem;
    height: auto;
    margin: 3rem 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    border-radius: 1rem;
    padding: 1rem;
    /* background-color: var(--azul_padrao); */
    box-shadow: 0px 0px 7px 0px rgba(0,70,97,0.65);
}

.box_item_titulo {
    margin: 0 0 1rem 0;
    font-size: 2rem;
    letter-spacing: .1rem;
    font-weight: 500;
    color: var(--azul_padrao);
}


.box_item_imagem{
    display: flex   ;
    width: 70%;
    height: 22rem;
    background-color: #EFEFEF;
    align-items: center;
    justify-content: center;
}

.box_item_imagem img {
    height: 100%;
}



.box_item_descricao {
    width: 90%;
    margin: 0 0 1rem 0;
}

.box_item_descricao p {
    font-family: 'Montserrat', sans-serif;
    padding: 1rem 0;
    font-size: 1.4rem;
    font-weight: 400;
    color: var(--azul_escuro);
}

.box_item_descricao_lista p{
    font-family: 'Montserrat', sans-serif;
    padding: 0;
    /* font-weight: 700; */
}

.box_item_descricao_lista {
    padding-left: 2rem;
    color: var(--azul_escuro);
}

#footer_projeto {
    margin-top: 1rem;
}

#footer_projeto p {
    font-weight: bold;
    padding: 1rem;
    color: var(--azul_escuro);
}